<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MeerkatMusic" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MeerkatMusic" parent="Base.Theme.MeerkatMusic">
        <item name="background">@color/black</item>
        <item name="textColor">@color/white</item>
        <item name="colorPrimaryDark">@color/gentleBlack</item>
        <item name="colorPrimary">@color/darkGray</item>
        <item name="imagePlay">@drawable/play_dark</item>
        <item name="backgroundColorNowPlying">@color/darkGray</item>
    </style>
    <style name="NoStatusBar" parent="Theme.MeerkatMusic">
        <item name="imagePlay">@drawable/play_dark</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
</resources>